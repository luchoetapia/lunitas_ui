import { Stack, Typography } from '@mui/material'

// App entry point — placeholder content for initial project setup.
function App() {
    return (
        <Stack spacing={1} sx={{ p: 4 }}>
            <Typography variant="h1">Hola Mundo</Typography>
            <Typography variant="subtitle1">Lunitas App</Typography>
            <Typography variant="body2">Configuracion inicial del proyecto.</Typography>
        </Stack>
    )
}

export default App
