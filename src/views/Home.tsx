import { Stack, Typography } from '@mui/material'

function Home() {
    return (
        <Stack spacing={1} sx={{ p: 4 }}>
            <Typography variant="h1">Inicio</Typography>
            <Typography variant="body2">Pagina de inicio de Lunitas Deco Hogar.</Typography>
        </Stack>
    )
}

export default Home
